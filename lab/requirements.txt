# Laboratoire multi-stratégies — Python 3.11+
pandas>=2.0
numpy>=1.26
pyarrow>=14.0          # cache parquet
pandas_market_calendars>=4.4  # séances NYSE et TSX
requests>=2.31
tabulate>=0.9
pytest>=8.0

# Fournisseurs de données
yfinance>=0.2.40       # quotidien, recherche seulement
ib_insync>=0.9.86      # IBKR : nécessite TWS ou IB Gateway en mode papier
